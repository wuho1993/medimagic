begin;

-- Ensure referenced positions exist
insert into public.positions (code, name_zh, name_en)
values
  ('ACCOUNT_MANAGER', '會計經理', 'Account Manager'),
  ('ASSISTANT_SHOP_MANAGER', '副店長', 'Assistant Shop Manager'),
  ('BEAUTICIAN', '美容師', 'Beautician'),
  ('CONSULTANT', '顧問', 'Consultant'),
  ('DESIGNER', '設計師', 'Designer'),
  ('MANAGER', '經理', 'Manager'),
  ('MARKETING_EXECUTIVE', '行銷主管', 'Marketing Executive'),
  ('MASSAGIST', '養生師', 'Massagist'),
  ('NURSE', '護士', 'Nurse'),
  ('OFFICE_CLERK', '文員', 'Office Clerk'),
  ('OPERATION_MANAGER', '營運經理', 'Operation Manager'),
  ('RECEPTIONIST', '接待員', 'Receptionist'),
  ('SALES_MANAGER', '銷售經理', 'Sales Manager'),
  ('SENIOR_BEAUTICIAN', '高級美容師', 'Senior Beautician'),
  ('SHOP_MANAGER', '店長', 'Shop Manager'),
  ('TRAINER', '培訓老師', 'Trainer')
on conflict (code) do update set
  name_zh = excluded.name_zh,
  name_en = excluded.name_en;


-- Employee upserts from V6 reconciliation
insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF182',
  '陳家寶',
  'Chan Ka Po',
  'BoYi',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y117412(4)',
  date '1990-07-21',
  '新界天水圍俊宏軒6座22室',
  '60600219',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TM' limit 1),
  'TM',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'RECEPTIONIST' limit 1),
  date '2022-06-02',
  'V6 匯入 2026-03；來源 row 2。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF068',
  '焦麗萍',
  'Chiu Lai Ping',
  'Canice',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y021950(7)',
  date '1989-03-20',
  '富亨邨亨榮樓1403室',
  '67316009',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'RECEPTIONIST' limit 1),
  date '2020-07-13',
  'V6 匯入 2026-03；來源 row 3。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF342',
  '楊美美',
  'Yeung May May',
  'May 姐',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'C274465(A)',
  date '1956-09-25',
  '荃灣大窩口邨富德樓1樓13室',
  '98628843',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2019-11-11',
  'V6 匯入 2026-03；來源 row 4。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF155',
  '吳嘉敏',
  'NG KA MAN CARMAN',
  'Carman',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z539776(7)',
  date '1980-10-01',
  'Flat A, 11/F, Blk 6, Piorhead Gdn, 168 Wu Chui Rd, Tuen Mun',
  '97102336',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'TRAINER' limit 1),
  date '2021-10-16',
  'V6 匯入 2026-03；來源 row 5。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF156',
  '馬玉雯',
  'Ma Yuk Man',
  'Sunny',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'R179085(3)',
  date '1996-09-06',
  '東涌滿東邨滿康樓24樓2411室',
  '67081514',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MARKETING_EXECUTIVE' limit 1),
  date '2021-10-01',
  'V6 匯入 2026-03；來源 row 6。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF188',
  '廖詩盈',
  'LIU SZE YING',
  'Helen',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'V127902(5)',
  date '1996-12-10',
  '油麻地渡船街252號富景洋樓26F/C座',
  '51818948',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'DESIGNER' limit 1),
  date '2022-06-27',
  'V6 匯入 2026-03；來源 row 7。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF222',
  '區寶欣',
  'Au Po Yan',
  'Penny',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z335626(5)',
  date '1983-10-23',
  'Flat B, 20/F, Block 20, City One Statin 1 Tak Wing Street, Shatin, N.T.',
  '61069237',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'TRAINER' limit 1),
  date '2022-12-01',
  'V6 匯入 2026-03；來源 row 8。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF233',
  '黃碧儀',
  'Wong Pik Yee',
  'Tracy',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y846228(1)',
  date '2002-06-11',
  '大埔翠屏花園A座一樓八室',
  '55790622',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'RECEPTIONIST' limit 1),
  date '2023-04-27',
  'V6 匯入 2026-03；來源 row 9。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF339',
  '余敏慧',
  'Yu Man Wai',
  'Fanny',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y048581(9)',
  date '1988-12-16',
  'Falt E, 5/F,Blk 2 , Broadview Gdn,Tsing Yi',
  '65303020',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'OFFICE_CLERK' limit 1),
  date '2024-11-18',
  'V6 匯入 2026-03；來源 row 10。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF280',
  '何文頤',
  'Ho Man Yee',
  'Amanda',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y382696(A)',
  date '1994-12-27',
  'Flat A,16/F,Harkfield Building, 8Smithfield Road,Kenndy Town',
  '60644515',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'RECEPTIONIST' limit 1),
  date '2024-04-12',
  'V6 匯入 2026-03；來源 row 11。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF282',
  '黃惠燕',
  'Wong Wai Yin',
  'Vincci',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z408146(4)',
  date '1981-04-16',
  '青衣青康路10號青盛苑1510室',
  '63808403',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'NURSE' limit 1),
  date '2024-05-08',
  'V6 匯入 2026-03；來源 row 12。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF292',
  '連嘉雯',
  'Lin Ka Man',
  'Mon',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z871828(9)',
  date '1987-09-20',
  '沙田大圍隆亨邨學心樓2樓209室',
  '90527088',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2024-06-01',
  'V6 匯入 2026-03；來源 row 13。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF313',
  '楊達偉',
  'Yeung Tat Wai',
  'Barry',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z472686(4)',
  date '1983-05-09',
  'Flat H, 5/F, Blk 2, Fu Ning Garden, 25 Po Ning Road, TKO',
  '96262425',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'ACCOUNT_MANAGER' limit 1),
  date '2024-07-23',
  'V6 匯入 2026-03；來源 row 14。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF318',
  '陳熾燊',
  'Chan Chi San',
  'Santo',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z068047(9)',
  date '1980-12-13',
  null,
  '93745571',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'OPERATION_MANAGER' limit 1),
  date '2024-08-01',
  'V6 匯入 2026-03；來源 row 15。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF337',
  '黃婉君',
  'Wong Yuen Kwan',
  'Monica',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'R80474(3)',
  date '1998-11-17',
  '粉嶺華心邨華冠樓3014室',
  '55135963',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'RECEPTIONIST' limit 1),
  date '2024-11-04',
  'V6 匯入 2026-03；來源 row 16。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF006',
  '鄭家彤',
  'Cheng Ka Tung',
  'KT',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y270111A',
  date '1992-09-24',
  '新界大埔大埔中心18座15樓H室',
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'SENIOR_BEAUTICIAN' limit 1),
  date '2016-12-12',
  'V6 匯入 2026-03；來源 row 28。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF249',
  'Tai Mei Mei',
  'Tai Mei Mei',
  'Maymay',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'K766222(A)',
  null,
  '九龍城城南道68號2樓F室',
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2023-08-11',
  'V6 匯入 2026-03；來源 row 29。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF025',
  '黃芊銢',
  'Wong Chin Wing',
  'Winnie',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'R441938(2)',
  date '1989-03-20',
  '沙田康林苑景林閣2307室',
  '98645403',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'SENIOR_BEAUTICIAN' limit 1),
  date '2017-12-01',
  'V6 匯入 2026-03；來源 row 30。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF066',
  '溫樂心',
  'Wan Lok Sum',
  'Yan',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'P514901(4)',
  date '1989-07-23',
  '九龍石硤尾邨23座1302室',
  '64053998',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2019-07-15',
  'V6 匯入 2026-03；來源 row 31。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF102',
  '溫婉華',
  'Wan Yuen Wa',
  'Carol',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'V112473(0)',
  date '1989-08-22',
  'Flat D, 17/F, Tower One, Twin Peaks, TKO',
  '63901341',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2020-09-07',
  'V6 匯入 2026-03；來源 row 32。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF014',
  '沈雯美',
  'Shum Man Mei',
  'Jess',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'K564713(4)',
  date '1957-02-15',
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MASSAGIST' limit 1),
  date '2017-02-03',
  'V6 匯入 2026-03；來源 row 33。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF065',
  '班秀珍',
  'Ban Xiuzhen',
  '珍珍',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'M649553(8)',
  date '1968-01-14',
  '九龍城城南道68號2樓F室',
  '55411189',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MASSAGIST' limit 1),
  date '2019-05-03',
  'V6 匯入 2026-03；來源 row 34。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF247',
  '賀蓉花',
  'He Ronghua',
  'Anna',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'M454095(1)',
  date '1979-03-17',
  '馬鞍山利安邨利華樓2309室',
  '63064650',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MASSAGIST' limit 1),
  date '2023-08-01',
  'V6 匯入 2026-03；來源 row 35。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF117',
  '張穎詩',
  'Cheung Wendy',
  'Wendy',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'K396005(6)',
  date '1975-03-30',
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2021-02-27',
  'V6 匯入 2026-03；來源 row 36。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF164',
  '楊樂兒',
  'Yeung Lok Yi',
  'Lok Yi',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y289319(1)',
  date '1992-06-23',
  'RM1801,YAT SANG HOUSE, SIU HIN COURT, TUEN MUN',
  '62712555',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TM' limit 1),
  'TM',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2017-12-01',
  'V6 匯入 2026-03；來源 row 40。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF144',
  '楊詠兒',
  'Yeung Wing Yi',
  'Wing',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y511410(4)',
  date '1994-12-14',
  'Room 1723 Oi Fai House, Yau Oi Estate, Tuen Mum',
  '67684188',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TM' limit 1),
  'TM',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2021-09-01',
  'V6 匯入 2026-03；來源 row 41。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF211',
  '徐慧玲',
  'Chui Wai Ling',
  '細Ling',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y434812(3)',
  null,
  null,
  '55115746',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TM' limit 1),
  'TM',
  '兼職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2022-09-02',
  'V6 匯入 2026-03；來源 row 42。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF196',
  '李迎迎',
  'Li Ying Ying',
  '(B)婷婷',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'F024215(4)',
  date '1989-08-15',
  '天水圍天瑞邨瑞林樓709室',
  '96258910',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TM' limit 1),
  'TM',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2022-07-19',
  'V6 匯入 2026-03；來源 row 43。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF146',
  '陳潔湘',
  'Chan Kit Sheung',
  'Emily',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z194767(3)',
  date '1984-01-19',
  'Rm 3208, Lung Tat Hse Lower, Wong Tai Sin',
  '60481158',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2021-09-03',
  'V6 匯入 2026-03；來源 row 44。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF119',
  '王龍欣',
  'Wong Lung Yan',
  'Yan Wong',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'R725739(1)',
  date '1992-01-02',
  '青衣清心街鹽田角村34號3樓',
  '91625959',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2021-03-01',
  'V6 匯入 2026-03；來源 row 49。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF279',
  '王嬌',
  'Wang Jiao',
  'KITTY',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'M535934(7)',
  date '1985-08-14',
  '葵涌葵盛東邨盛安樓1912室',
  '68712298',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MASSAGIST' limit 1),
  date '2024-04-04',
  'V6 匯入 2026-03；來源 row 50。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF243',
  'Liu Shu Yan',
  'Liu Shu Yan',
  'Chilly',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2023-07-01',
  'V6 匯入 2026-03；來源 row 52。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF272',
  '邱少碧',
  'Qiu Shaobi',
  'Jojo',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'R184303(5)',
  date '1962-10-15',
  '荃灣梨木樹邨翠樹樓3013室',
  '51721328',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MASSAGIST' limit 1),
  date '2024-01-19',
  'V6 匯入 2026-03；來源 row 57。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF145',
  '梁捷瑩',
  '梁捷瑩',
  'Alice',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z239447(3)',
  date '1982-02-16',
  '香港西灣河筲箕灣道92-102號業寧大廈10樓H室',
  '97816132',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2021-07-07',
  'V6 匯入 2026-03；來源 row 59。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF206',
  '江儉',
  'Kong Kim',
  'Water(PT)',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'H462407(8)',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2022-08-16',
  'V6 匯入 2026-03；來源 row 61。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF277',
  '高鳳香',
  'GAO Feng Xiang',
  'Joey',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'R333773(0)',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2024-03-14',
  'V6 匯入 2026-03；來源 row 62。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF170',
  '李佩英',
  'Lee Pui Ying',
  'Moon(B)',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'D469961(1)',
  date '1959-06-26',
  'Flat B, 12/F, Blk 3, Grandway Gdn, Shatin',
  '94689533',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MASSAGIST' limit 1),
  date '2022-04-21',
  'V6 匯入 2026-03；來源 row 67。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF199',
  '梁玉嬋',
  'Leung Yuk Sim',
  '呀晴',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'K362389(0)',
  date '1974-08-17',
  'FLAT 9 37/F, BLK D, LUNG POON COUR, LUNG KEI HOUSE, DIAMOND HILL, KOWLOON.',
  '91704116',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2022-08-01',
  'V6 匯入 2026-03；來源 row 69。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF334',
  '徐狄美',
  'Tsui Tik Mei',
  'Maymay',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y606363(0)',
  date '1997-07-23',
  'Rm10,11/F, Blk5,Mei Lok Hse,Mei Tin Est, Tai Wai NT, HK',
  '94243161',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2024-10-21',
  'V6 匯入 2026-03；來源 row 70。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF220',
  '郭麗萍',
  'Guo LiPing',
  'Pinky',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'R709407(7)',
  date '1979-03-28',
  '石硤尾白田村太田樓1814室',
  '90907339',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2022-11-01',
  'V6 匯入 2026-03；來源 row 71。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF241',
  '梁頌慈',
  'Leung Chung Chi',
  'Rachel',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y616699(5)',
  date '1997-05-29',
  '屯門富健花園9座18樓K室',
  '66412541',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'MOS' limit 1),
  'MOS',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2023-06-07',
  'V6 匯入 2026-03；來源 row 73。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF193',
  '林碧珠',
  'Lam Pik Chu',
  '雯雯',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'P354517(6)',
  date '1967-02-14',
  '黃大仙蒲崗村道75號富祐大廈3C',
  '62711052',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKCY' limit 1),
  'MKCY',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2022-07-07',
  'V6 匯入 2026-03；來源 row 77。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF312',
  '林映明',
  'Lam Ying Ming',
  'MING',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'F463321(7)',
  date '1992-11-24',
  '葵芳邨葵明樓15樓13室',
  '60509821',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKCY' limit 1),
  'MKCY',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2024-07-09',
  'V6 匯入 2026-03；來源 row 78。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF238',
  '張月峙琦',
  'Cheung Yuet Si Kei',
  'Kei',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'P623734(0)',
  date '1967-01-18',
  '沙田馬鞍山錦泰苑錦天閣1003室',
  '54148878',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MASSAGIST' limit 1),
  date '2023-05-08',
  'V6 匯入 2026-03；來源 row 79。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF011',
  '李瑞斯',
  'Lee Sui Sze Sylvia',
  'Sylvia',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'C672456(4)',
  date '1971-12-24',
  'Flat C,31/F,Tower2, One East Coast, 1 Lei Yue Mun Path, Yau Tong, Kowloon,HK',
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'SALES_MANAGER' limit 1),
  date '2016-12-19',
  'V6 匯入 2026-03；來源 row 83。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF190',
  '鄒善盈',
  'Chow Sin Ying Dorcas',
  'Dorcas',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y021740(7)',
  date '1989-01-16',
  '元朗坑屋村129號1樓',
  '65887466',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'ASSISTANT_SHOP_MANAGER' limit 1),
  date '2022-06-28',
  'V6 匯入 2026-03；來源 row 84。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF134',
  '朱詠賢',
  'Chu Wing Yin',
  'Jacqueline',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'K964736(8)',
  date '1979-05-12',
  '將軍澳蔚藍灣畔五座43樓F室',
  '65022002',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MANAGER' limit 1),
  date '2021-05-01',
  'V6 匯入 2026-03；來源 row 85。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF173',
  '何嘉慧',
  'Ho Ka Wai',
  'Karrie',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z819778(5)',
  date '1981-01-27',
  '深水埗海壇街218號愛海頌1座17/F B室',
  '66288546',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'CONSULTANT' limit 1),
  date '2022-04-22',
  'V6 匯入 2026-03；來源 row 86。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF137',
  '陳泳芝',
  'Chan Wing Chi',
  'Yena',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Y346086(8)',
  date '1993-08-14',
  '坑口厚德邨德志樓2118室',
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'CONSULTANT' limit 1),
  date '2021-07-01',
  'V6 匯入 2026-03；來源 row 87。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF154',
  '毛曉春',
  'Mo Hiu Chun',
  'Lanke',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'V062304(0)',
  date '1985-04-14',
  '屯門福亨村215號地下',
  '91727370',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TM' limit 1),
  'TM',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'SHOP_MANAGER' limit 1),
  date '2021-10-01',
  'V6 匯入 2026-03；來源 row 88。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF185',
  '陳凱螢',
  'Chan Hoi Ying',
  'Iris',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'z241337(0)',
  date '1982-02-16',
  'Rm C8, 31/F, BLK E, Garden Rivera, Shatin, NT',
  '97254926',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'MANAGER' limit 1),
  date '2021-04-21',
  'V6 匯入 2026-03；來源 row 90。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF172',
  '陳欣欣',
  'Chan Yan Yan',
  'YY',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'P841845(8)',
  date '1986-06-19',
  'G/F, No.55 Prosperous Villa, Tai Hang Village, Tai Po',
  '65758276',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'BEAUTICIAN' limit 1),
  date '2022-04-21',
  'V6 匯入 2026-03；來源 row 91。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF323',
  '費曉蓮',
  'Fei Xiao Lian',
  'Candy',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'R808067(3)',
  date '1982-02-08',
  '沙田馬鞍山150號錦駿苑E座3604室',
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MOS' limit 1),
  'MOS',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'CONSULTANT' limit 1),
  date '2024-09-03',
  'V6 匯入 2026-03；來源 row 92。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF167',
  '曾瑞卿',
  'Tsang Sui Hing',
  'Ella',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'G816457(5)',
  date '1971-04-06',
  null,
  '61931944',
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'SALES_MANAGER' limit 1),
  date '2024-11-01',
  'V6 匯入 2026-03；來源 row 93。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF335',
  '丘沛然',
  'YAU PUI YIN',
  'ZOE',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z378318(A)',
  date '1981-08-15',
  '大圍顯徑邨顯貴樓2613室/大圍嘉田苑嘉詠閣1208室',
  '97078121',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'CONSULTANT' limit 1),
  date '2024-10-25',
  'V6 匯入 2026-03；來源 row 94。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF129',
  '劉麗霞',
  'LAU LAI HAR',
  'Jackie',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    'Z545263(6)',
  date '1980-11-20',
  '屯門富健花園10座21樓H室',
  '92012353',
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = 'OPERATION_MANAGER' limit 1),
  date '2021-04-12',
  'V6 匯入 2026-03；來源 row 96。已對到員工資料。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R017',
  'Tammy',
  'Tammy',
  'Tammy',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = null limit 1),
  null,
  '兼職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 17。V6 暫時只有資料：兼職。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R018',
  'Hebe',
  'Hebe',
  'Hebe',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '兼職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 18。V6 暫時只有資料：兼職。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF356',
  'Yu (Office)',
  'Yu (Office)',
  'Yu (Office)',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 19。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF368',
  'Yana(MKCYCS)',
  'Yana(MKCYCS)',
  'Yana(MKCYCS)',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKCY' limit 1),
  'MKCY',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 20。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF372',
  'Costte',
  'Costte',
  'Costte',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'OFFICE' limit 1),
  'OFFICE',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 21。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R022',
  'Ashley',
  'Ashley',
  'Ashley',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 22。V6 內 staff no 重複：SF370 / Ashley。原始 V6 staff no：SF370。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R023',
  '塋姐(大圍清潔)',
  '塋姐(大圍清潔)',
  '塋姐(大圍清潔)',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '兼職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 23。V6 暫時只有資料：兼職。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R024',
  '珍姐(MOS清潔)',
  '珍姐(MOS清潔)',
  '珍姐(MOS清潔)',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MOS' limit 1),
  'MOS',
  '兼職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 24。V6 暫時只有資料：兼職。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF376',
  'Tiffany',
  'Tiffany',
  'Tiffany',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKTOP' limit 1),
  'MKTOP',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 37。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF378',
  '英',
  '英',
  '英',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TM' limit 1),
  'TM',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 45。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF189',
  'JUMBO',
  'JUMBO',
  'JUMBO',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 48。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF355',
  'Gigi',
  'Gigi',
  'Gigi',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 51。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF360',
  'Joyce',
  'Joyce',
  'Joyce',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 53。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R054',
  'Yu',
  'Yu',
  'Yu',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 54。V6 內 staff no 重複：SF370 / Yu。原始 V6 staff no：SF370。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R058',
  'Sugar',
  'Sugar',
  'Sugar',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 58。V6 有 staff no，但員工資料有重複資料列。原始 V6 staff no：SF265。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R060',
  'KIKI (CWB)',
  'KIKI (CWB)',
  'KIKI (CWB)',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '兼職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 60。V6 內未能確認：KIKI。原始 V6 staff no：SF146。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF322',
  'Icy',
  'Icy',
  'Icy',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'CWB' limit 1),
  'CWB',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 63。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF358',
  'Maggie',
  'Maggie',
  'Maggie',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MOS' limit 1),
  'MOS',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 74。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF216',
  'Lilly',
  'Lilly',
  'Lilly',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MOS' limit 1),
  'MOS',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 75。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF377',
  '玉',
  '玉',
  '玉',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'MKCY' limit 1),
  'MKCY',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 80。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF186',
  'Moon',
  'Moon',
  'Moon',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = 'TAIWAI' limit 1),
  'TAIWAI',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 89。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'SF375',
  'Alferd',
  'Alferd',
  'Alferd',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASAS'::public.employee_company_type,
  (select id from public.companies where code = 'ASAS' limit 1),
  (select id from public.branches where code = 'TW' limit 1),
  'TW',
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 95。V6 有資料，但員工資料沒有此 staff no。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R097',
  'Dr Chung',
  'Dr Chung',
  'Dr Chung',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = null limit 1),
  null,
  '兼職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 97。V6 暫時只有資料：兼職。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R098',
  'Sun Xiaojie',
  'Sun Xiaojie',
  'Sun Xiaojie',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = null limit 1),
  null,
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 98。V6 暫時只有資料：外勞。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R099',
  'Xue Minglan',
  'Xue Minglan',
  'Xue Minglan',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = null limit 1),
  null,
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 99。V6 暫時只有資料：外勞。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R100',
  'Dan Wu',
  'Dan Wu',
  'Dan Wu',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = null limit 1),
  null,
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 100。V6 暫時只有資料：街霸。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R101',
  'Lilly',
  'Lilly',
  'Lilly',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = null limit 1),
  null,
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 101。V6 暫時只有資料：街霸。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

insert into public.employees (
  employee_code,
  name_zh,
  name_en,
  alias,
  gender,
  identity_type,
  identity_number,
  date_of_birth,
  address,
  phone,
  company_type,
  company_id,
  branch_id,
  branch_code,
  employment_type,
  employment_status,
  position_id,
  hire_date,
  notes
)
values (
  'V6TMP202603R102',
  'Wu Anru',
  'Wu Anru',
  'Wu Anru',
  'other'::public.employee_gender,
  'hkid'::public.employee_identity_type,
    '',
  null,
  null,
  null,
  'ASA'::public.employee_company_type,
  (select id from public.companies where code = 'ASA' limit 1),
  (select id from public.branches where code = null limit 1),
  null,
  '全職'::public.employee_employment_type,
  'active'::public.employee_status,
  (select id from public.positions where code = null limit 1),
  date '2026-03-01',
  'V6 匯入 2026-03；來源 row 102。V6 暫時只有資料：街霸。入職日缺失，暫以 2026-03-01 佔位。'
)
on conflict (employee_code) do update set
  name_zh = coalesce(nullif(excluded.name_zh, ''), employees.name_zh),
  name_en = coalesce(nullif(excluded.name_en, ''), employees.name_en),
  alias = coalesce(nullif(excluded.alias, ''), employees.alias),
  identity_number = case when excluded.identity_number is null then employees.identity_number else excluded.identity_number end,
  date_of_birth = coalesce(excluded.date_of_birth, employees.date_of_birth),
  address = coalesce(nullif(excluded.address, ''), employees.address),
  phone = coalesce(nullif(excluded.phone, ''), employees.phone),
  company_type = coalesce(excluded.company_type, employees.company_type),
  company_id = coalesce(excluded.company_id, employees.company_id),
  branch_id = coalesce(excluded.branch_id, employees.branch_id),
  branch_code = coalesce(nullif(excluded.branch_code, ''), employees.branch_code),
  employment_type = coalesce(excluded.employment_type, employees.employment_type),
  employment_status = coalesce(excluded.employment_status, employees.employment_status),
  position_id = coalesce(excluded.position_id, employees.position_id),
  hire_date = coalesce(excluded.hire_date, employees.hire_date),
  notes = case when excluded.notes is null or excluded.notes = '' then employees.notes when employees.notes is null or employees.notes = '' then excluded.notes when position(excluded.notes in employees.notes) > 0 then employees.notes else employees.notes || E'\n' || excluded.notes end,
  updated_at = timezone('utc', now());

-- Salary profile upserts from V6 reconciliation
insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF182' limit 1),
  'monthly'::public.employee_salary_type,
  14500,
  date '2022-06-02',
  'V6 2026-03 匯入；row 2；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF068' limit 1),
  'monthly'::public.employee_salary_type,
  17200,
  date '2020-07-13',
  'V6 2026-03 匯入；row 3；已對到員工資料',
  true,
  1000,
  1000,
  1000,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF342' limit 1),
  'monthly'::public.employee_salary_type,
  10427,
  date '2019-11-11',
  'V6 2026-03 匯入；row 4；已對到員工資料',
  false,
  0,
  null,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF155' limit 1),
  'monthly'::public.employee_salary_type,
  27000,
  date '2021-10-16',
  'V6 2026-03 匯入；row 5；已對到員工資料',
  true,
  1000,
  1000,
  1000,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF156' limit 1),
  'monthly'::public.employee_salary_type,
  24500,
  date '2021-10-01',
  'V6 2026-03 匯入；row 6；已對到員工資料',
  true,
  1000,
  null,
  1000,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF188' limit 1),
  'monthly'::public.employee_salary_type,
  20500,
  date '2022-06-27',
  'V6 2026-03 匯入；row 7；已對到員工資料',
  true,
  1000,
  null,
  1000,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF222' limit 1),
  'monthly'::public.employee_salary_type,
  33000,
  date '2022-12-01',
  'V6 2026-03 匯入；row 8；已對到員工資料',
  true,
  1000,
  1000,
  1000,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF233' limit 1),
  'monthly'::public.employee_salary_type,
  13000,
  date '2023-04-27',
  'V6 2026-03 匯入；row 9；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF339' limit 1),
  'monthly'::public.employee_salary_type,
  15500,
  date '2024-11-18',
  'V6 2026-03 匯入；row 10；已對到員工資料',
  true,
  1000,
  null,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF280' limit 1),
  'monthly'::public.employee_salary_type,
  15000,
  date '2024-04-12',
  'V6 2026-03 匯入；row 11；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF282' limit 1),
  'monthly'::public.employee_salary_type,
  22000,
  date '2024-05-08',
  'V6 2026-03 匯入；row 12；已對到員工資料',
  true,
  2000,
  null,
  1000,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF292' limit 1),
  'monthly'::public.employee_salary_type,
  17000,
  date '2024-06-01',
  'V6 2026-03 匯入；row 13；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF313' limit 1),
  'monthly'::public.employee_salary_type,
  42500,
  date '2024-07-23',
  'V6 2026-03 匯入；row 14；已對到員工資料',
  true,
  1000,
  2000,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF318' limit 1),
  'monthly'::public.employee_salary_type,
  33500,
  date '2024-08-01',
  'V6 2026-03 匯入；row 15；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF337' limit 1),
  'monthly'::public.employee_salary_type,
  12200,
  date '2024-11-04',
  'V6 2026-03 匯入；row 16；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF006' limit 1),
  'monthly'::public.employee_salary_type,
  3750,
  date '2016-12-12',
  'V6 2026-03 匯入；row 28；已對到員工資料',
  true,
  1000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF249' limit 1),
  'monthly'::public.employee_salary_type,
  8500,
  date '2023-08-11',
  'V6 2026-03 匯入；row 29；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF025' limit 1),
  'monthly'::public.employee_salary_type,
  3900,
  date '2017-12-01',
  'V6 2026-03 匯入；row 30；已對到員工資料',
  true,
  1000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF066' limit 1),
  'monthly'::public.employee_salary_type,
  21300,
  date '2019-07-15',
  'V6 2026-03 匯入；row 31；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  '睇後面跳Bar"500起跳"'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF102' limit 1),
  'monthly'::public.employee_salary_type,
  7200,
  date '2020-09-07',
  'V6 2026-03 匯入；row 32；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF014' limit 1),
  'monthly'::public.employee_salary_type,
  10200,
  date '2017-02-03',
  'V6 2026-03 匯入；row 33；已對到員工資料',
  true,
  2000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF065' limit 1),
  'monthly'::public.employee_salary_type,
  7200,
  date '2019-05-03',
  'V6 2026-03 匯入；row 34；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF247' limit 1),
  'monthly'::public.employee_salary_type,
  7200,
  date '2023-08-01',
  'V6 2026-03 匯入；row 35；已對到員工資料',
  true,
  2000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF117' limit 1),
  'monthly'::public.employee_salary_type,
  6300,
  date '2021-02-27',
  'V6 2026-03 匯入；row 36；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF164' limit 1),
  'monthly'::public.employee_salary_type,
  20500,
  date '2017-12-01',
  'V6 2026-03 匯入；row 40；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  '睇後面跳Bar"500起跳"'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF144' limit 1),
  'monthly'::public.employee_salary_type,
  8000,
  date '2021-09-01',
  'V6 2026-03 匯入；row 41；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF211' limit 1),
  'daily'::public.employee_salary_type,
  1000,
  date '2022-09-02',
  'V6 2026-03 匯入；row 42；已對到員工資料',
  true,
  1000,
  null,
  0,
  0,
  '無REDEM, SALES 15萬以下X3% 15以上X3.5% ,JOB錢問LANKE拿,每3個月一次1500 ,每年8月出10000'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF196' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2022-07-19',
  'V6 2026-03 匯入；row 43；已對到員工資料',
  true,
  2000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF146' limit 1),
  'monthly'::public.employee_salary_type,
  6500,
  date '2021-09-03',
  'V6 2026-03 匯入；row 44；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF119' limit 1),
  'monthly'::public.employee_salary_type,
  8300,
  date '2021-03-01',
  'V6 2026-03 匯入；row 49；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF279' limit 1),
  'monthly'::public.employee_salary_type,
  7200,
  date '2024-04-04',
  'V6 2026-03 匯入；row 50；已對到員工資料',
  true,
  2000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF243' limit 1),
  'monthly'::public.employee_salary_type,
  9000,
  date '2023-07-01',
  'V6 2026-03 匯入；row 52；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF272' limit 1),
  'monthly'::public.employee_salary_type,
  7300,
  date '2024-01-19',
  'V6 2026-03 匯入；row 57；已對到員工資料',
  true,
  2000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF145' limit 1),
  'monthly'::public.employee_salary_type,
  8000,
  date '2021-07-07',
  'V6 2026-03 匯入；row 59；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF206' limit 1),
  'monthly'::public.employee_salary_type,
  3700,
  date '2022-08-16',
  'V6 2026-03 匯入；row 61；已對到員工資料',
  true,
  1000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF277' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2024-03-14',
  'V6 2026-03 匯入；row 62；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  'Joey 底薪$6500👉🏻$7000，包薪$16500👉🏻$18500 1/12開始'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF170' limit 1),
  'monthly'::public.employee_salary_type,
  19300,
  date '2022-04-21',
  'V6 2026-03 匯入；row 67；已對到員工資料',
  true,
  1000,
  null,
  0,
  0,
  'JOB $2000 ,無REDEM + SALES'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF199' limit 1),
  'monthly'::public.employee_salary_type,
  6800,
  date '2022-08-01',
  'V6 2026-03 匯入；row 69；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF334' limit 1),
  'monthly'::public.employee_salary_type,
  6200,
  date '2024-10-21',
  'V6 2026-03 匯入；row 70；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF220' limit 1),
  'monthly'::public.employee_salary_type,
  7200,
  date '2022-11-01',
  'V6 2026-03 匯入；row 71；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF241' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2023-06-07',
  'V6 2026-03 匯入；row 73；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  '低過12萬成份糧8折 ,11開始底薪由6500升7000,20萬以上包30000 ,低過12萬22400 , 正常28000'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF193' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2022-07-07',
  'V6 2026-03 匯入；row 77；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF312' limit 1),
  'monthly'::public.employee_salary_type,
  7200,
  date '2024-07-09',
  'V6 2026-03 匯入；row 78；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF238' limit 1),
  'monthly'::public.employee_salary_type,
  7200,
  date '2023-05-08',
  'V6 2026-03 匯入；row 79；已對到員工資料',
  true,
  2000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF011' limit 1),
  'monthly'::public.employee_salary_type,
  15300,
  date '2016-12-19',
  'V6 2026-03 匯入；row 83；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF190' limit 1),
  'monthly'::public.employee_salary_type,
  17000,
  date '2022-06-28',
  'V6 2026-03 匯入；row 84；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF134' limit 1),
  'monthly'::public.employee_salary_type,
  20500,
  date '2021-05-01',
  'V6 2026-03 匯入；row 85；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  '睇群組鋪數X %'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF173' limit 1),
  'monthly'::public.employee_salary_type,
  14000,
  date '2022-04-22',
  'V6 2026-03 匯入；row 86；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF137' limit 1),
  'monthly'::public.employee_salary_type,
  13500,
  date '2021-07-01',
  'V6 2026-03 匯入；row 87；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  '睇群組鋪數X % , 無個人BONUS'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF154' limit 1),
  'monthly'::public.employee_salary_type,
  13500,
  date '2021-10-01',
  'V6 2026-03 匯入；row 88；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  '睇群組鋪數X %'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF185' limit 1),
  'monthly'::public.employee_salary_type,
  15500,
  date '2021-04-21',
  'V6 2026-03 匯入；row 90；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  'SALES輸入大圍鋪數, under 80萬 3%,$80萬-@100萬3.2%,'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF172' limit 1),
  'monthly'::public.employee_salary_type,
  15500,
  date '2022-04-21',
  'V6 2026-03 匯入；row 91；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  'SALES輸入大圍鋪數X1.8%'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF323' limit 1),
  'monthly'::public.employee_salary_type,
  9000,
  date '2024-09-03',
  'V6 2026-03 匯入；row 92；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF167' limit 1),
  'monthly'::public.employee_salary_type,
  13300,
  date '2024-11-01',
  'V6 2026-03 匯入；row 93；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF335' limit 1),
  'monthly'::public.employee_salary_type,
  12200,
  date '2024-10-25',
  'V6 2026-03 匯入；row 94；已對到員工資料',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF129' limit 1),
  'monthly'::public.employee_salary_type,
  55000,
  date '2021-04-12',
  'V6 2026-03 匯入；row 96；已對到員工資料',
  true,
  2000,
  0,
  1000,
  0,
  '鋪數總數X0.1% , JK Job上限5%'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R017' limit 1),
  'hourly'::public.employee_salary_type,
  85,
  date '2026-03-01',
  'V6 2026-03 匯入；row 17；V6 暫時只有資料：兼職',
  false,
  null,
  null,
  null,
  null,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R018' limit 1),
  'hourly'::public.employee_salary_type,
  95,
  date '2026-03-01',
  'V6 2026-03 匯入；row 18；V6 暫時只有資料：兼職',
  false,
  null,
  null,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF356' limit 1),
  'monthly'::public.employee_salary_type,
  13000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 19；V6 有資料，但員工資料沒有此 staff no',
  true,
  1000,
  null,
  0,
  0,
  '40人以下,以 $40/人 ,41-80人,以 $50/人 ,80人以上,以$60/人'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF368' limit 1),
  'monthly'::public.employee_salary_type,
  12500,
  date '2026-03-01',
  'V6 2026-03 匯入；row 20；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF372' limit 1),
  'monthly'::public.employee_salary_type,
  14500,
  date '2026-03-01',
  'V6 2026-03 匯入；row 21；V6 有資料，但員工資料沒有此 staff no',
  true,
  1000,
  null,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R022' limit 1),
  'monthly'::public.employee_salary_type,
  14000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 22；V6 內 staff no 重複：SF370 / Ashley',
  true,
  2000,
  null,
  500,
  500,
  '40人以下,以 $40/人 ,41-80人,以 $50/人 ,80人以上,以$60/人'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R023' limit 1),
  'hourly'::public.employee_salary_type,
  100,
  date '2026-03-01',
  'V6 2026-03 匯入；row 23；V6 暫時只有資料：兼職',
  false,
  null,
  null,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R024' limit 1),
  'hourly'::public.employee_salary_type,
  85,
  date '2026-03-01',
  'V6 2026-03 匯入；row 24；V6 暫時只有資料：兼職',
  false,
  null,
  null,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF376' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 37；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF378' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 45；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF189' limit 1),
  'monthly'::public.employee_salary_type,
  5200,
  date '2026-03-01',
  'V6 2026-03 匯入；row 48；V6 有資料，但員工資料沒有此 staff no',
  true,
  1000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF355' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 51；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  250,
  250,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF360' limit 1),
  'monthly'::public.employee_salary_type,
  6000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 53；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R054' limit 1),
  'monthly'::public.employee_salary_type,
  6000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 54；V6 內 staff no 重複：SF370 / Yu',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R058' limit 1),
  'monthly'::public.employee_salary_type,
  6500,
  date '2026-03-01',
  'V6 2026-03 匯入；row 58；V6 有 staff no，但員工資料有重複資料列',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R060' limit 1),
  'daily'::public.employee_salary_type,
  1000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 60；V6 內未能確認：KIKI',
  false,
  null,
  null,
  null,
  null,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF322' limit 1),
  'monthly'::public.employee_salary_type,
  7300,
  date '2026-03-01',
  'V6 2026-03 匯入；row 63；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF358' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 74；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF216' limit 1),
  'monthly'::public.employee_salary_type,
  7000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 75；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF377' limit 1),
  'monthly'::public.employee_salary_type,
  8000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 80；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF186' limit 1),
  'monthly'::public.employee_salary_type,
  15500,
  date '2026-03-01',
  'V6 2026-03 匯入；row 89；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  'SALES輸入大圍鋪數, under 80萬 3%,$80萬-@100萬3.2%,'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'SF375' limit 1),
  'monthly'::public.employee_salary_type,
  12000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 95；V6 有資料，但員工資料沒有此 staff no',
  true,
  2000,
  null,
  500,
  500,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R097' limit 1),
  'hourly'::public.employee_salary_type,
  2400,
  date '2026-03-01',
  'V6 2026-03 匯入；row 97；V6 暫時只有資料：兼職',
  false,
  null,
  null,
  null,
  null,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R098' limit 1),
  'monthly'::public.employee_salary_type,
  20000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 98；V6 暫時只有資料：外勞',
  false,
  null,
  null,
  null,
  null,
  '每月2萬(外勞)'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R099' limit 1),
  'monthly'::public.employee_salary_type,
  20000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 99；V6 暫時只有資料：外勞',
  false,
  null,
  null,
  null,
  null,
  '每月2萬(外勞)'
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R100' limit 1),
  'monthly'::public.employee_salary_type,
  16500,
  date '2026-03-01',
  'V6 2026-03 匯入；row 100；V6 暫時只有資料：街霸',
  true,
  2000,
  null,
  0,
  0,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R101' limit 1),
  'monthly'::public.employee_salary_type,
  16000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 101；V6 暫時只有資料：街霸',
  true,
  2000,
  null,
  null,
  null,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

insert into public.employee_salary_profiles (
  employee_id,
  salary_type,
  base_salary,
  effective_from,
  remarks,
  attendance_bonus_enabled,
  attendance_bonus_amount,
  transport_allowance,
  briefing_bonus,
  booking_bonus,
  commission_notes
)
values (
  (select id from public.employees where employee_code = 'V6TMP202603R102' limit 1),
  'monthly'::public.employee_salary_type,
  18000,
  date '2026-03-01',
  'V6 2026-03 匯入；row 102；V6 暫時只有資料：街霸',
  true,
  2000,
  null,
  null,
  null,
  null
)
on conflict (employee_id) do update set
  salary_type = coalesce(excluded.salary_type, employee_salary_profiles.salary_type),
  base_salary = coalesce(excluded.base_salary, employee_salary_profiles.base_salary),
  effective_from = coalesce(excluded.effective_from, employee_salary_profiles.effective_from),
  attendance_bonus_enabled = coalesce(excluded.attendance_bonus_enabled, employee_salary_profiles.attendance_bonus_enabled),
  attendance_bonus_amount = coalesce(excluded.attendance_bonus_amount, employee_salary_profiles.attendance_bonus_amount),
  transport_allowance = coalesce(excluded.transport_allowance, employee_salary_profiles.transport_allowance),
  briefing_bonus = coalesce(excluded.briefing_bonus, employee_salary_profiles.briefing_bonus),
  booking_bonus = coalesce(excluded.booking_bonus, employee_salary_profiles.booking_bonus),
  remarks = case when excluded.remarks is null or excluded.remarks = '' then employee_salary_profiles.remarks when employee_salary_profiles.remarks is null or employee_salary_profiles.remarks = '' then excluded.remarks when position(excluded.remarks in employee_salary_profiles.remarks) > 0 then employee_salary_profiles.remarks else employee_salary_profiles.remarks || E'\n' || excluded.remarks end,
  commission_notes = case when excluded.commission_notes is null or excluded.commission_notes = '' then employee_salary_profiles.commission_notes when employee_salary_profiles.commission_notes is null or employee_salary_profiles.commission_notes = '' then excluded.commission_notes when position(excluded.commission_notes in employee_salary_profiles.commission_notes) > 0 then employee_salary_profiles.commission_notes else employee_salary_profiles.commission_notes || E'\n' || excluded.commission_notes end,
  updated_at = timezone('utc', now());

commit;